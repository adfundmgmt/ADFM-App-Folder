name: Web Platform

on:
  pull_request:
    paths:
      - "apps/**"
      - "docker-compose.web.yml"
      - ".github/workflows/web-platform.yml"
  push:
    branches:
      - main
    paths:
      - "apps/**"
      - "docker-compose.web.yml"
      - ".github/workflows/web-platform.yml"

jobs:
  api:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install API dependencies
        run: python -m pip install -r apps/api/requirements.txt
      - name: Compile API
        run: python -m compileall -q apps/api/app adfm_core/rate_of_change.py
      - name: Import API application
        run: python -c "from apps.api.app.main import app; print(app.title)"

  web:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/web
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "npm"
          cache-dependency-path: apps/web/package.json
      - name: Install frontend dependencies
        run: npm install
      - name: Build frontend
        env:
          NEXT_PUBLIC_ADFM_API_URL: http://localhost:8000
        run: npm run build
